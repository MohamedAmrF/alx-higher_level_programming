#!/usr/bin/python3


def replace_in_list(my_list, idx, element):
    newlist = mylist.copy()
    if idx < 0:
        return new_list
    if idx >= len(my_list):
        return new_list
    new_list[idx] = element
    return new_list
